import { PokeAPI } from './client';
import {
    PokeAPIResource,
    PokePaginatedResult,
    PokePaginationParams,
    PokeResource,
    PokeUnnamedResource,
} from './types';

export { PokeAPI, PokeAPIResource };

export type { PokePaginatedResult, PokePaginationParams, PokeResource, PokeUnnamedResource };
